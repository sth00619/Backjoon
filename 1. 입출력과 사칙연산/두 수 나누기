// 다른 사칙 연산과 다르게 정수 형태가 아니라 실수 형태로 출력
A, B = input().split()
A = float(A)
B = float(B)

// 그냥 출력해도 되지만 소수 9자리까지 오차를 인정한다는 설명이 있어 계산 결과를 소수점 9째 자리까지 구할 수 있도록 round를 이용했다
#print(A/B)
print(round(a/b,9))
