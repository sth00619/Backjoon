// 처음에는 복잡하게 두번째 입력받은 수의 나머지나 몫을 가지고 조작하는 방법을 생각했다.
// 두번째 수는 첫번째 수와 다르게 입력받을 때 int로 형변환을 해주지 않고 받는다.
// 인덱스의 기능을 활용하기 위함인데, 인덱싱을 하여 첫번째 수와 곱할 때 int 형변환을 해주면 되는 문제

A=int(input())
B=input()

print(A*int(B[2]))
print(A*int(B[1]))
print(A*int(B[0]))
print(A*int(B))
