// 체스 규칙 상 필요한 종류별 체스말의 개수
// 리스트 형태로 담는다.
chess = [1,1,2,2,2,8]

// 현재 가지고 있는 종류별 체스말의 개수
// input을 통해 입력을 받은 후에, 사칙연산에서 사용한 것처럼 split을 이용하여 배열 형태로 담는다.
now = list(map(int,input().split()))

// for문을 이용하여 0번 인덱스부터 5번 인덱스까지 6번 반복하여 원래 필요한 말의 개수와 현재 가지고 있는 말의 개수의 차이를 구한다.
for i in range(6):
    print(chess[i]-now[i], end=" ")
